{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gulps.example.com/manifest.schema.json",
  "title": "GULPS Activity Manifest",
  "description": "Manifest for a GULPS learning activity, declaring capabilities and values.",
  "type": "object",
  "required": ["name", "client"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Unique identifier for the activity."
    },
    "client": {
      "type": "string",
      "description": "Path to the client-side JavaScript file, relative to activity directory."
    },
    "server": {
      "type": "string",
      "description": "Path to the server-side WASM file, relative to activity directory."
    },
    "capabilities": {
      "type": "object",
      "description": "Declared capabilities that the sandbox can use.",
      "additionalProperties": false,
      "properties": {
        "http": {
          "description": "HTTP request capability.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "allowed_hosts": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Allowed hostnames. Empty array allows all hosts."
            }
          }
        },
        "ai": {
          "description": "AI model access capability.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "models": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Allowed model names. Empty array allows all models."
            }
          }
        }
      }
    },
    "values": {
      "type": "object",
      "description": "Declared activity values with type and scope.",
      "additionalProperties": {
        "$ref": "#/$defs/valueDefinition"
      }
    },
    "actions": {
      "type": "object",
      "description": "Actions the client can send to the server sandbox.",
      "additionalProperties": { "$ref": "#/$defs/typeSchema" }
    },
    "events": {
      "type": "object",
      "description": "Events the server sandbox can emit to the client. values.change.* events are implicit.",
      "additionalProperties": { "$ref": "#/$defs/typeSchema" }
    },
    "static": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^(?!/)(?!.*\\.\\.).+$"
      },
      "description": "Static file paths that can be served as assets. Must be relative (no leading '/') and cannot contain '..'."
    }
  },
  "$defs": {
    "valueDefinition": {
      "type": "object",
      "required": ["type", "scope"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["integer", "number", "string", "boolean", "array", "object"],
          "description": "Data type of the value. Uses JSON Schema type names."
        },
        "items": {
          "$ref": "#/$defs/typeSchema",
          "description": "Schema for array items. Required when type is 'array'."
        },
        "properties": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/typeSchema" },
          "description": "Schema for object properties. Used when type is 'object'."
        },
        "scope": {
          "type": "string",
          "enum": ["unit", "user,unit"],
          "description": "Scope of the value. 'unit' is shared across users, 'user,unit' is per-user."
        },
        "default": {
          "description": "Default value (must match declared type)."
        }
      }
    },
    "typeSchema": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["integer", "number", "string", "boolean", "array", "object"],
          "description": "Data type."
        },
        "items": {
          "$ref": "#/$defs/typeSchema",
          "description": "Schema for array items."
        },
        "properties": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/typeSchema" },
          "description": "Schema for object properties."
        }
      }
    }
  }
}
